# Copy to `.env` and fill in. `.env` is gitignored and must stay that way —
# this repository is public.
#
#   cp .env.example .env
#
# Key from https://platform.claude.com/settings/keys, on Maria's PERSONAL
# account. Nothing in this project may touch the Taylor & Francis identity.
#
# Only the trace-recording scripts read this. The published site is static and
# makes no API calls, so a visitor can never spend this key.
ANTHROPIC_API_KEY=sk-ant-...
