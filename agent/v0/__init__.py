"""Tiny Agent v0 — lesson 2. A loop, three tools, a turn limit.

You write `loop.py` and the tool bodies in `tools.py`. `__main__.py` (the runner)
and `agent/harness/` (the recorder and fixtures) are provided. Run it with:

    uv run python -m agent.v0
"""
