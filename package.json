{
  "name": "tiny-agents",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run check:secrets && npm run check:sources && npm run check:lessons && npm run check:style && npm run check:contrast",
    "check:secrets": "node scripts/check-secrets.mjs",
    "check:sources": "node scripts/check-sources.mjs",
    "check:lessons": "node scripts/check-lessons.mjs",
    "check:style": "node scripts/check-style.mjs",
    "check:contrast": "node scripts/check-contrast.mjs",
    "check:pointers": "node scripts/check-pointers.mjs"
  },
  "dependencies": {
    "@astrojs/react": "^6.0.4",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "astro": "^7.2.6",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zod": "^4.4.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0"
  }
}
